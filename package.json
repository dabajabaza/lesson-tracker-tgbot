{
  "name": "lesson-tracker-bot",
  "private": true,
  "type": "module",
  "description": "LessonTracker42Bot — Telegram-бот учёта оплат учеников (Telegram Serverless)",
  "scripts": {
    "deploy": "tgcloud push",
    "status": "tgcloud status",
    "run": "tgcloud run",
    "local": "node local/run.mjs",
    "test": "node --import ./test/register.mjs test/test.mjs && node --no-warnings --import ./test/register-local.mjs test/test.mjs"
  },
  "devDependencies": {
    "@tgcloud/cli": "^0.1.2"
  }
}
