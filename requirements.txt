aiogram>=3.7,<4
SQLAlchemy>=2.0,<3
alembic>=1.13
aiosqlite>=0.20
openpyxl>=3.1
# нужен aiogram для работы через прокси (TELEGRAM_PROXY), в т.ч. http-прокси
aiohttp-socks>=0.8
# Для перехода на PostgreSQL (п.19 ТЗ) — раскомментировать и указать
# DATABASE_URL=postgresql+asyncpg://user:pass@host/db
# asyncpg>=0.29
