"""Ключи в data — общий словарь апдейта, через который middleware
договариваются между собой.

Отдельным модулем, а не внутри одного из middleware: ACCESS_DENIED пишет
контроль доступа, а читает единица работы. Положив ключ к любой из сторон, мы
бы завели зависимость между ними ради одной строковой константы.
"""

# Апдейт отброшен контролем доступа и обработчика не видел. Живёт в общем
# словаре, потому что обе стороны — inner-middleware одного обсервера
# dp.update и получают буквально один и тот же объект.
ACCESS_DENIED = "access_denied"
