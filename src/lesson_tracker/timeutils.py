"""Единые часы приложения: одна функция «сейчас» на весь пакет.

Лежит в корне пакета, а не в domain/: время читает и db/models.py (как
default колонок), и services/, и runtime/outbox.py. Любой подпакет здесь
развернул бы стрелку зависимости в обратную сторону — db/ начал бы
импортировать domain/ или runtime/. Модуль не импортирует ничего своего и
поэтому лежит ниже всех слоёв.
"""

import time


def now_ts() -> int:
    return int(time.time())
